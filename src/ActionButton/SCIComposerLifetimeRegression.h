#import <Foundation/Foundation.h>

// Marker header retained with SCIComposerLifetimeRegression.m so future source
// syncs do not silently drop the ARC lifetime regression coverage.
FOUNDATION_EXPORT NSString * const SCIComposerLifetimeRegressionMarker;
